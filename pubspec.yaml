name: assignment5
description: แอปพลิเคชันจัดการสินค้า REST API
publish_to: 'none' # ไม่ต้องเผยแพร่ไป pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0    # สำหรับเรียก REST API

  # Material Design (มีอยู่แล้วใน Flutter)
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    # หากมีไฟล์รูปภาพ/JSON เพิ่มตรงนี้
    # - assets/images/
